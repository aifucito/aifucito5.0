// utils/ubicacion.js
export async function obtenerCoordenadas(direccion) {
  // Función mínima de prueba para pruebas locales
  // Devuelve coordenadas ficticias
  console.log(`Obteniendo coordenadas para: ${direccion}`);
  return { lat: -34.90328, lng: -56.18816 };
}